# Consolidated Docker workflow

This file is now just a short pointer to the main runbook.

Use [LOCAL-DEV-RUNBOOK.md](LOCAL-DEV-RUNBOOK.md) as the authoritative guide for:

- starting the shared stack
- running migrations for each app
- seeding each app individually
- starting only one app instead of all
- troubleshooting the local environment

The root compose file remains the canonical local-development entry point in [docker-compose.yml](docker-compose.yml).
