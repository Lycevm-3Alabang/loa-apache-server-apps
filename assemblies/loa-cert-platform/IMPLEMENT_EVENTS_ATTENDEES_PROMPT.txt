# IMPLEMENTATION PROMPT: Events & Attendees Controllers for LOA Cert Platform

## CONTEXT
You are completing the **Events** and **Attendees** resource groups for the LOA Cert Platform (`assemblies/loa-cert-platform/cert-app/`). This is **Phase C** of the Cert Platform — the unauthenticated domain CRUD slice (auth middleware `jwt.auth`/`jwt.endpoint` and SSO are deferred per decision #20/D9).

**Specs are FINAL** — code exactly to them:
- `api-endpoints.md` v1.4 (Final) — §5.1 Events (13 endpoints), §5.2 Attendees (8 endpoints)
- `business-contexts/certificate/event.md` (Final) — Event aggregate spec
- `business-contexts/certificate/event-attendee.md` (Final) — EventAttendee aggregate spec

**DO NOT** implement auth, SSO, QR codes, email sending, or anything not in the spec.

---

## CURRENT STATE (as of 2026-08-08)

### ✅ ALREADY IMPLEMENTED — EventController (Basic CRUD + Stats Stub)
| Endpoint | Status | Notes |
|----------|--------|-------|
| `GET /api/v1/events` | ✅ Done | List with search, status, pagination |
| `POST /api/v1/events` | ✅ Done | Create with validation |
| `GET /api/v1/events/{id}` | ✅ Done | Show single |
| `PATCH /api/v1/events/{id}` | ✅ Done | Partial update |
| `DELETE /api/v1/events/{id}` | ✅ Done | Delete (cascades) |
| `GET /api/v1/events/{id}/stats` | ⚠️ **Stub** | Returns **mock data** — needs real implementation |

**File:** `app/Http/Controllers/EventController.php` (149 lines)
**Tests:** `tests/Unit/EventControllerTest.php` (basic CRUD only)

### ❌ MISSING — EventController (Advanced Endpoints)
| Endpoint | Spec Ref | Priority |
|----------|----------|----------|
| `POST /api/v1/events/{id}/clone-template` | §5.1.7 | High |
| `POST /api/v1/events/{id}/clone-email-template` | §5.1.8 | High |
| `POST /api/v1/events/{id}/bulk-issue` | §5.1.9 | High |
| `POST /api/v1/events/{id}/reissue` | §5.1.10 | High |
| `GET /api/v1/events/{id}/revoke-expired` | §5.1.11 | High |
| `POST /api/v1/events/{id}/revoke-expired` | §5.1.12 | High |
| `POST /api/v1/events/{id}/issue-completed` | §5.1.13 | High |

### ❌ COMPLETELY MISSING — AttendeeController (8 endpoints)
All endpoints from §5.2 need implementation.

### ❌ MISSING — Routes & OpenAPI
- **Routes not registered** in `routes/api.php` (Events + Attendees completely absent)
- **No OpenAPI attributes** on EventController (unlike Certificate/Template controllers)

---

## GUARD RAILS — PREVENT HALLUCINATION

| Rule | Enforcement |
|------|-------------|
| **Spec-first** | Read `api-endpoints.md` §5.1 and §5.2 completely before writing ANY code. The spec is the source of truth. |
| **Follow existing patterns** | Copy patterns from `CertificateController.php` and `CertificateTemplateController.php` — same response envelopes, validation, error handling, OpenAPI attributes. |
| **No auth middleware** | Controllers have NO middleware. Routes are registered WITHOUT `jwt.auth`/`jwt.endpoint`. Auth is deferred. |
| **No `created_by` logic yet** | The `created_by` field exists in migrations but author-scoping is handled by the `jwt.endpoint` middleware later. Controllers just accept/save the field from request attributes if present. |
| **Use existing services** | `PdfService`, `PlaceholderResolver` exist. Do NOT create new services. |
| **Test patterns** | Follow `CertificateTest.php` and `CertificateTemplateTest.php` exactly — `RefreshDatabase`, `setUp()` creates org + config, `assertJsonStructure`, `assertJsonPath`. |
| **Run tests** | After each controller, run tests. They MUST pass. |
| **Preserve existing CRUD** | Do NOT rewrite the 6 existing EventController methods — only ADD the 7 missing methods and fix `stats()`. |

---

## EXISTING CODE PATTERNS TO COPY

### 1. Response Envelope (from CertificateController)
```php
// Success — single resource
return response()->json(['data' => $this->formatCertificate($certificate)], 201);

// Success — collection
return response()->json([
    'data' => $certificates,
    'meta' => ['limit' => $limit, 'offset' => $offset, 'total' => $total, 'has_more' => ($offset + $limit) < $total],
]);

// Error
return response()->json([
    'status' => 'error',
    'message' => 'Human-readable summary',
    'errors' => ['field' => ['rule broken']], // optional
], 422); // or 404, 409, 401, 403
```

### 2. Validation Pattern
```php
$validator = Validator::make($request->all(), [
    'field' => 'required|string|max:255',
    'email' => 'required|email',
    // ...
]);
if ($validator->fails()) {
    throw ValidationException::withMessages($validator->errors()->toArray());
}
```

### 3. Not Found Pattern
```php
$model = Model::find($id);
if (!$model) {
    return response()->json([
        'status' => 'error',
        'message' => 'Model not found.',
    ], 404);
}
```

### 4. Organization Resolution (from both controllers)
```php
private function resolveOrganizationId(): string
{
    return config('cert-platform.organization_id', '00000000-0000-0000-0000-000000000001');
}
```

### 5. OpenAPI Attributes (from CertificateController)
```php
#[OA\Get(
    path: "/api/v1/certificates",
    summary: "List certificates",
    tags: ["Certificates"],
    parameters: [
        new OA\Parameter(name: "event_id", in: "query", schema: new OA\Schema(type: "string", format: "uuid")),
        // ...
    ],
    responses: [
        new OA\Response(response: 200, description: "Success", content: new OA\JsonContent(ref: "#/components/schemas/CertificateListResponse")),
        new OA\Response(response: 401, description: "Unauthorized"),
    ]
)]
```

### 6. Test Pattern (from CertificateTemplateTest)
```php
protected function setUp(): void
{
    parent::setUp();
    $this->organization = Organization::create(['name' => 'Lyceum of Alabang', 'slug' => 'loa']);
    config(['cert-platform.organization_id' => $this->organization->id]);
}

public function test_list_templates(): void
{
    CertificateTemplate::create([...]);
    $response = $this->getJson('/api/v1/templates');
    $response->assertStatus(200)
        ->assertJsonStructure([...])
        ->assertJsonPath('meta.total', 1);
}
```

---

## IMPLEMENTATION PLAN — STEP BY STEP

### STEP 1: Complete EventController.php
**File:** `app/Http/Controllers/EventController.php` — **ADD to existing file**

**Add these 7 missing methods + fix `stats()`:**

| Method | Spec Ref | Key Logic |
|--------|----------|-----------|
| `cloneTemplate(Request $request, string $id)` | §5.1.7 | Validate source template exists + type=certificate; create new template from source; attach as event's `template_id` if not set; return `{template_id, name}` |
| `cloneEmailTemplate(Request $request, string $id)` | §5.1.8 | Same as clone-template but for type=email; attach as `email_template_id` |
| `bulkIssue(Request $request, string $id)` | §5.1.9 | For each `attendee_ids[]`: generate cert number atomically (reuse `CertificateController::generateCertificateNumber`), create Certificate, render PDF, link attendee, optionally send email; return bulk result shape |
| `reissue(Request $request, string $id)` | §5.1.10 | For each `attendee_ids[]`: revoke existing cert + issue new number; admin-only per spec |
| `revokeExpiredCount(string $id)` | §5.1.11 | Count expired certs for event (revoked_at=null, expires_at < now) |
| `revokeExpired(string $id)` | §5.1.12 | Revoke all expired certs for event (set revoked_at=now, revoke_reason='Auto-expired') |
| `issueCompleted(Request $request, string $id)` | §5.1.13 | Issue certs for attendees with `completed=true`, optionally filtered by `attendee_ids[]` |

**Fix `stats()` method** — replace mock data with real counts:
```php
$attendees = $event->attendees();
$certificates = $event->certificates(); // via relationship
return response()->json([
    'data' => [
        'event_id' => $event->id,
        'attendees' => [
            'total' => $attendees->count(),
            'attended' => $attendees->where('attended', true)->count(),
            'completed' => $attendees->where('completed', true)->count(),
        ],
        'certificates' => [
            'issued' => $certificates->count(),
            'active' => $certificates->whereNull('revoked_at')->where(function($q){ $q->whereNull('expires_at')->orWhere('expires_at', '>=', now()); })->count(),
            'revoked' => $certificates->whereNotNull('revoked_at')->count(),
            'expired' => $certificates->whereNull('revoked_at')->whereNotNull('expires_at')->where('expires_at', '<', now())->count(),
        ],
        'expiring' => $certificates->whereNull('revoked_at')->whereNotNull('expires_at')->where('expires_at', '>=', now())->where('expires_at', '<=', now()->addDays(30))->count(),
    ]
]);
```

**Add OpenAPI attributes** to ALL methods (including existing 6) — copy pattern from CertificateController.

**Add required OpenAPI schemas at top of controller:**
- `Event`, `EventListResponse`, `EventSingleResponse`
- `EventCreateRequest`, `EventUpdateRequest`
- `CloneTemplateRequest` (source_template_id, name)
- `BulkIssueRequest` (attendee_ids[], send_email)
- `ReissueRequest` (attendee_ids[])
- `IssueCompletedRequest` (send_email, attendee_ids?)
- `EventStatsResponse` (per spec §5.1.6)

**Models to use:** `Event`, `CertificateTemplate`, `Certificate`, `EventAttendee`, `CertificateSequence`
**Services:** Inject `PdfService` in constructor (for bulk-issue PDF generation)

---

### STEP 2: Create AttendeeController.php
**File:** `app/Http/Controllers/AttendeeController.php` — **NEW FILE**

**Implement all 8 endpoints from §5.2:**

| Method | Path | Spec Ref | Key Logic |
|--------|------|----------|-----------|
| `index(Request $request, string $event_id)` | `GET /events/{event_id}/attendees` | §5.2.1 | List with filters: search, attended, completed, has_certificate, pagination |
| `store(Request $request, string $event_id)` | `POST /events/{event_id}/attendees` | §5.2.2 | Upsert by (event_id, email) — duplicate email updates existing |
| `import(Request $request, string $event_id)` | `POST /events/{event_id}/attendees/import` | §5.2.3 | JSON payload; `mode=merge` (upsert) or `replace` (clear+insert, requires `confirm=true`); return `{imported, skipped, errors[]}` |
| `update(Request $request, string $id)` | `PATCH /attendees/{id}` | §5.2.4 | Partial update |
| `destroy(string $id)` | `DELETE /attendees/{id}` | §5.2.5 | Remove attendee, unlink certificate (set certificate_id=null) |
| `destroyWithCertificate(string $id)` | `DELETE /attendees/{id}/with-cert` | §5.2.6 | Remove attendee AND delete linked certificate |
| `deletePreview(string $id)` | `GET /attendees/{id}/delete-preview` | §5.2.7 | Return impact: linked certificate info if exists |
| `fileData(string $id)` | `GET /attendees/{id}/file-data` | §5.2.8 | Return PDF bytes if `generation_mode=file`, else `{data: {generation_mode: "template"}}` |

**Add OpenAPI attributes** to ALL methods + schemas at top:
- `Attendee`, `AttendeeListResponse`, `AttendeeSingleResponse`
- `AttendeeCreateRequest`, `AttendeeUpdateRequest`
- `AttendeeImportRequest` (attendees[], mode, confirm)
- `ImportResult`, `DeletePreviewResponse`

**Models:** `EventAttendee`, `Event`, `Certificate`

---

### STEP 3: Register Routes in routes/api.php
**File:** `routes/api.php` — **ADD to existing file**

```php
use App\Http\Controllers\EventController;
use App\Http\Controllers\AttendeeController;

// ... existing routes ...

// Events
Route::prefix('events')->group(function () {
    Route::get('/', [EventController::class, 'index']);
    Route::post('/', [EventController::class, 'store']);
    Route::get('/{id}', [EventController::class, 'show']);
    Route::patch('/{id}', [EventController::class, 'update']);
    Route::delete('/{id}', [EventController::class, 'destroy']);
    Route::get('/{id}/stats', [EventController::class, 'stats']);
    Route::post('/{id}/clone-template', [EventController::class, 'cloneTemplate']);
    Route::post('/{id}/clone-email-template', [EventController::class, 'cloneEmailTemplate']);
    Route::post('/{id}/bulk-issue', [EventController::class, 'bulkIssue']);
    Route::post('/{id}/reissue', [EventController::class, 'reissue']);
    Route::get('/{id}/revoke-expired', [EventController::class, 'revokeExpiredCount']);
    Route::post('/{id}/revoke-expired', [EventController::class, 'revokeExpired']);
    Route::post('/{id}/issue-completed', [EventController::class, 'issueCompleted']);

    // Nested attendees
    Route::prefix('{event_id}/attendees')->group(function () {
        Route::get('/', [AttendeeController::class, 'index']);
        Route::post('/', [AttendeeController::class, 'store']);
        Route::post('/import', [AttendeeController::class, 'import']);
    });
});

// Attendees (standalone by attendee id)
Route::prefix('attendees')->group(function () {
    Route::patch('/{id}', [AttendeeController::class, 'update']);
    Route::delete('/{id}', [AttendeeController::class, 'destroy']);
    Route::delete('/{id}/with-cert', [AttendeeController::class, 'destroyWithCertificate']);
    Route::get('/{id}/delete-preview', [AttendeeController::class, 'deletePreview']);
    Route::get('/{id}/file-data', [AttendeeController::class, 'fileData']);
});
```

---

### STEP 4: Write Tests
**Files:** `tests/Feature/Api/EventTest.php`, `tests/Feature/Api/AttendeeTest.php` — **NEW FILES** (Feature tests, not Unit)

**Follow patterns from:**
- `CertificateTemplateTest.php` (CRUD + locking tests)
- `CertificateTest.php` (bulk operations, status derivation, edge cases)

**Test coverage required:**
- **EventTest:** All 13 endpoints — list (filters, pagination), create (validation, required pattern, template type checks), get, update, delete, **stats (real counts)**, clone-template, clone-email-template, bulk-issue, reissue, revoke-expired (count + action), issue-completed
- **AttendeeTest:** All 8 endpoints — list (filters), create (upsert by email), import (merge + replace with confirm, validation errors), update, destroy, destroy-with-cert, delete-preview, file-data

**Use `RefreshDatabase` trait. Set up organization + event + template in `setUp()`.**

---

### STEP 5: Run Tests & Verify
```bash
cd assemblies/loa-cert-platform/cert-app
php artisan test --filter=EventTest
php artisan test --filter=AttendeeTest
```

**All tests MUST pass.** Fix any failures before considering complete.

---

## SNIPPETS FROM EXISTING CODE TO REFERENCE

### CertificateController::generateCertificateNumber (reuse this!)
```php
private function generateCertificateNumber(string $organizationId, string $pattern): string
{
    return DB::transaction(function () use ($organizationId, $pattern) {
        $sequence = CertificateSequence::lockForUpdate()
            ->firstOrCreate(
                ['organization_id' => $organizationId, 'pattern' => $pattern],
                ['next_value' => 1]
            );

        $value = $sequence->next_value;
        $sequence->increment('next_value');

        $width = substr_count($pattern, '#');
        $paddedValue = str_pad($value, $width, '0', STR_PAD_LEFT);

        $number = str_replace('####', $paddedValue, $pattern);
        $number = str_replace('YYYY', date('Y'), $number);

        return $number;
    });
}
```

### CertificateTemplateController::isTemplateLocked (pattern for checking references)
```php
private function isTemplateLocked(CertificateTemplate $template): bool
{
    return Event::where('template_id', $template->id)
        ->orWhere('email_template_id', $template->id)
        ->exists();
}
```

### PdfService usage (for bulk-issue PDF generation)
```php
try {
    $this->pdfService->generateCertificatePdf($certificate->fresh(['event', 'template', 'organization']));
} catch (\Exception $e) {
    // Non-fatal
}
```

---

## WHAT NOT TO DO (COMMON HALLUCINATIONS)

| ❌ Don't Do | ✅ Do Instead |
|-------------|---------------|
| Rewrite existing 6 EventController methods | Only ADD 7 missing methods + fix `stats()` |
| Add `middleware(['jwt.auth', 'jwt.endpoint'])` to routes | Leave routes WITHOUT auth middleware (deferred) |
| Create new services (EmailService, QrService) | Use existing `PdfService` only; email/QR are deferred |
| Implement `created_by` author-scoping logic | Just save `$request->attributes->get('jwt_claims.sub')` if present; scoping is middleware's job |
| Add `cert.*` permission checks | No permission checks at all — auth is deferred |
| Use `PUT` for updates | Use `PATCH` for partial updates (spec §3.2) |
| Return base64 PDF in JSON | PDF endpoints return raw binary (spec §3.7) — but Events/Attendees don't have PDF endpoints |
| Generate certificate numbers without `CertificateSequence` | ALWAYS use `generateCertificateNumber` (atomic) |
| Default `certificate_number_pattern` | It's REQUIRED per spec — validate presence and `####` |
| Allow `auth_process` template type | Only `certificate` and `email` types (spec §2.2) |

---

## FILES TO MODIFY/CREATE

### Modify:
1. `app/Http/Controllers/EventController.php` — ADD 7 methods + fix stats() + add OpenAPI
2. `routes/api.php` — ADD Events + Attendees routes

### Create:
1. `app/Http/Controllers/AttendeeController.php` — NEW (8 methods + OpenAPI)
2. `tests/Feature/Api/EventTest.php` — NEW (Feature tests for all 13 endpoints)
3. `tests/Feature/Api/AttendeeTest.php` — NEW (Feature tests for all 8 endpoints)

---

## VERIFICATION CHECKLIST

Before declaring complete, verify:

- [ ] `EventController` has all 13 methods matching §5.1 endpoints (6 existing + 7 new + fixed stats)
- [ ] `AttendeeController` has all 8 methods matching §5.2 endpoints
- [ ] OpenAPI attributes on ALL methods (including existing 6) with correct paths, parameters, responses, schemas
- [ ] `routes/api.php` registers all 21 routes with correct prefixes
- [ ] `EventTest.php` has tests for all 13 endpoints (happy path + validation + edge cases)
- [ ] `AttendeeTest.php` has tests for all 8 endpoints
- [ ] All tests pass: `php artisan test --filter=EventTest` and `php artisan test --filter=AttendeeTest`
- [ ] No auth middleware on any route
- [ ] Certificate number generation uses `CertificateSequence` atomic transaction
- [ ] Bulk operations return per-item result shape (`success`, `failed`, `errors`, `certificates`)
- [ ] Response envelopes match spec (data + meta for collections, data for single, error envelope)
- [ ] `certificate_number_pattern` validation: required + contains `####`
- [ ] Template type validation: `certificate` for `template_id`, `email` for `email_template_id`
- [ ] Stats endpoint returns REAL counts (not mock data)

---

## RUN COMMANDS (for reference)

```bash
# From cert-app directory
cd assemblies/loa-cert-platform/cert-app

# Run specific test files
php artisan test tests/Feature/Api/EventTest.php
php artisan test tests/Feature/Api/AttendeeTest.php

# Run all tests
php artisan test

# Generate Swagger docs (after controllers done)
php artisan l5-swagger:generate
```

---

## QUESTIONS TO ASK IF BLOCKED

If you encounter ambiguity, STOP and ask:
1. "The spec says X but existing code does Y — which do I follow?" → Follow existing code patterns.
2. "Should I add auth middleware?" → NO, deferred per decision #20.
3. "How to handle `created_by`?" → Save from `$request->attributes->get('jwt_claims.sub')` if present, else null.
4. "What about email sending in bulk-issue?" → Spec says "optionally send email" — accept `send_email` flag but don't implement actual sending (deferred to Service Phase).

---

**IMPLEMENTATION ORDER:** 
1. Complete EventController (add 7 methods + fix stats + OpenAPI)
2. Create AttendeeController (8 methods + OpenAPI)
3. Register routes in routes/api.php
4. Write EventTest.php (Feature)
5. Write AttendeeTest.php (Feature)
6. Run Tests → Fix → Done.

Do not skip steps. Do not implement deferred features. Code exactly to the Final specs.